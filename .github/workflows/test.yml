name: Test
on:
  push:
jobs:
  job1:
    runs-on: ubuntu-latest
    steps:
      - run: cat /proc/cpuinfo
      - uses: actions/checkout@v3
      - run: ls -l $GITHUB_WORKSPACE
#      - run: wget -O mbsim.tar.bz2 https://www.mbsim-env.de/builds/run/4988/distributionFile/
#      - run: mkdir old
#      - run: (cd old; tar -xjf ../mbsim.tar.bz2)
#      - run: wget -O mbsim.tar.bz2 https://www.mbsim-env.de/builds/run/5018/distributionFile/
#      - run: mkdir new
#      - run: (cd new; tar -xjf ../mbsim.tar.bz2)
#      - run: old/mbsim-env/bin/mbsimxml test.mbsx
#      - run: new/mbsim-env/bin/mbsimxml test.mbsx
