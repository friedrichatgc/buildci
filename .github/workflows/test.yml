name: test
on:
  push:
jobs:
  build:
    name: "msys2 test"
    runs-on: windows-2019
    steps:
#      - name: "find on host"
#        run: Get-ChildItem -Path c:\ -Filter opengl32.dll -Recurse -ErrorAction SilentlyContinue -Force
      - name: "find in image"
        run: docker run mcr.microsoft.com/windows/servercore:ltsc2019 powershell.exe -Command "& {Get-ChildItem -Path c:\ -Filter opengl32.dll -Recurse -ErrorAction SilentlyContinue -Force}"
