name: Test
on:
  push:
jobs:
  job1:
    runs-on: ubuntu-latest
    steps:
      - run: touch abc
      - run: >
          shopt -s nullglob &&
          ls abc sdlkfj*
      - run: >
          ls abc sdlkfj*
