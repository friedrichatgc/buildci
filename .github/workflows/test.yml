name: test
on:
  push:
jobs:
  jobLinux:
    name: "jobLinux"
    runs-on: ubuntu-22.04
    steps:
      - name: "echo"
        run: >
          echo
          aaa
          ${{true && format('test{0}{1}', 'bbb', github.workspace) || ' '}}
          ccc
          ${{false && format('test{0}{1}', 'ddd', github.workspace) || ' '}}
          eee
  jobWindows:
    name: "jobWindows"
    runs-on: windows-2022
    steps:
      - name: "echo"
        run: >
          echo
          aaa
          ${{true && format('test{0}{1}', 'bbb', github.workspace) || ' '}}
          ccc
          ${{false && format('test{0}{1}', 'ddd', github.workspace) || ' '}}
          eee
