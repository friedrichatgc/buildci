name: Test
on:
  push:
jobs:
  job1:
    runs-on: ubuntu-latest
    if: {{always()}}
    steps:
      - run: echo job1a
        if: {{always()}}
      - run: ls filenotfound1
        if: {{always() && true}}
      - run: echo job1b
        if: {{always()}}
  job2:
    runs-on: ubuntu-latest
    if: {{always()}}
    steps:
      - run: echo job2a
        if: {{always()}}
      - run: ls filenotfound2
        if: {{always() && false}}
      - run: echo job2b
        if: {{always()}}
  job3:
    runs-on: ubuntu-latest
    if: {{always()}}
    needs: [job2]
    steps:
      - run: echo job3a
        if: {{always()}}
      - run: ls filenotfound3
        if: {{always()}}
      - run: echo job3b
        if: {{always()}}
