name: test
on:
  push:
jobs:
  build:
    name: "msys2 test"
    runs-on: windows-2019
    steps:
      - name: "RUN"
        run: docker run mcr.microsoft.com/windows/servercore:ltsc2019 notepad.exe
