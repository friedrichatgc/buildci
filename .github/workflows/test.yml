name: Create and publish a Docker image
on:
  push:
jobs:
  build-and-push-image:
    runs-on: windows-2019
    steps:
      - run: docker pull mbsimenv/proxy:latest
      - run: docker save mbsimenv/proxy:latest -o ${{github.workspace}}/image.tar
      - name: "Cache save: image"
        uses: actions/cache/save@v4
        with:
          path: ${{github.workspace}}/image.tar
          key: image
      #      - run: docker pull ghcr.io/friedrichatgc/proxy:latest
      #      - run: docker pull mbsimenv/proxy:latest
