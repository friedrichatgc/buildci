name: test
on:
  push:
jobs:
  build:
    name: "msys2 test"
    runs-on: windows-2019
    steps:
      - run: echo %PATH%
        if: ${{always()}}
      - run: which gh
        if: ${{always()}}
      - run: which gh
        if: ${{always()}}
