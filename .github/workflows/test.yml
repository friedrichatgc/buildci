name: Test
on:
  push:
jobs:
  job1:
    runs-on: windows-latest
    steps:
      - run: $CONDA\bin\conda install libblas
        if: ${{always()}}
      - run: $CONDA\bin\conda install conda-forge/libblas
        if: ${{always()}}
      - run: $CONDA\bin\conda activate base
        if: ${{always()}}
      - run: $CONDA\bin\conda install libblas
        if: ${{always()}}
      - run: $CONDA\bin\conda install conda-forge/libblas
        if: ${{always()}}
