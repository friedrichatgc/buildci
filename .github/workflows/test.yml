name: test
on:
  push:
jobs:
  build:
    name: "msys2 test"
    runs-on: windows-2019
    steps:
      - run: mkdir ${{github.workspace}}/dummy1
      - run: mkdir -p ${{github.workspace}}/dummy2
      - run: mkdir -m 777 ${{github.workspace}}/dummy3
      - run: mkdir -m 777 -p ${{github.workspace}}/dummy4
      - run: mkdir ${{github.workspace}}/dummy1/a
      - run: mkdir -p ${{github.workspace}}/dummy2/a
      - run: mkdir -m 777 ${{github.workspace}}/dummy3/a
      - run: mkdir -m 777 -p ${{github.workspace}}/dummy4/a
