name: Build Docker Images (Manual)
on:
  push:
jobs:
  buildImage:
    name: "Bulid Linux Docker Image"
    runs-on: ubuntu-22.04
    steps:
      - name: "name1"
        run: ls
      - name: "name2"
        continue-on-error: true
        run: ls notfound; exit 0
      - name: "name3"
        run: ls notfound
