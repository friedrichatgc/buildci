name: Test
on:
  push:
jobs:
  job1:
    runs-on: windows-latest
    steps:
      - run: conda install libblas
        if: ${{always()}}
      - run: conda install conda-forge/libblas
        if: ${{always()}}
      - run: conda activate base
        if: ${{always()}}
      - run: conda install libblas
        if: ${{always()}}
      - run: conda install conda-forge/libblas
        if: ${{always()}}
