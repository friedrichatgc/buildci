name: CI msys2 (Repo-Dispatch)
on:
  push:
jobs:
  build:
    name: "win64-ci-msys2"
    runs-on: windows-latest
    defaults:
      run:
        shell: C:\msys64\usr\bin\bash.exe {0}
    steps:
      - run: pwd
      - run: ls -l
      - run: ls -l /
      - run: echo $MSYSTEM
      - run: echo $PATH
